<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lineng.mapper.CatMapper">


    <select id="getCats" resultType="com.lineng.model.Cat" parameterType="java.lang.String">
        SELECT * FROM cat WHERE catName = #{catName}
    </select>

<!--    <insert id="insert" parameterType="com.lineng.model.Cat" >
        &lt;!&ndash;
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        &ndash;&gt;
        <selectKey resultType="java.lang.Integer" keyProperty="id" order="AFTER" >
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into cat (catAge, catName)
        values (#{catAge,jdbcType=VARCHAR}, #{catName,jdbcType=VARCHAR})
    </insert>-->
</mapper>